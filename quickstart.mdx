---
title: "Quick Start Guide"
description: "Get up and running with PinePay API in under 10 minutes"
---

# Quick Start Guide

This guide will walk you through setting up PinePay API integration in just a few steps. You'll be processing PIX payments in no time!

## Prerequisites

Before you begin, make sure you have:

- A PinePay account ([Sign up here](https://dashboard.pinepay.com))
- Your API credentials from the dashboard
- Node.js 16+ or your preferred programming language
- A webhook endpoint URL for receiving notifications

## Step 1: Install Dependencies

<CodeGroup>
```bash npm
npm install axios
```

```bash yarn
yarn add axios
```

```bash pip
pip install requests
```

```bash composer
composer require guzzlehttp/guzzle
```
</CodeGroup>

## Step 2: Configure Your Environment

Create a `.env` file with your PinePay credentials:

```bash .env
PINEPAY_API_KEY=your_api_key_here
PINEPAY_BASE_URL=https://api.pinepay.com
PINEPAY_WEBHOOK_URL=https://your-domain.com/webhooks/pinepay
```

<Warning>
Never commit your API keys to version control. Use environment variables or secure configuration management.
</Warning>

## Step 3: Make Your First API Call

Let's create a simple PIX payment request:

<CodeGroup>
```javascript Node.js
const axios = require('axios');

const createPixPayment = async () => {
  try {
    const response = await axios.post('https://api.pinepay.com/requestPayment', {
      amount: 10.50,
      pixKey: 'user@example.com',
      webhookUrl: 'https://your-domain.com/webhooks/pinepay',
      externalId: 'order_123',
      gateway: 'woovi',
      description: 'Payment for order #123'
    }, {
      headers: {
        'Authorization': `Bearer ${process.env.PINEPAY_API_KEY}`,
        'Content-Type': 'application/json'
      }
    });

    console.log('Payment created:', response.data);
    return response.data;
  } catch (error) {
    console.error('Error creating payment:', error.response?.data || error.message);
  }
};

createPixPayment();
```

```python Python
import requests
import os

def create_pix_payment():
    url = 'https://api.pinepay.com/requestPayment'
    headers = {
        'Authorization': f'Bearer {os.getenv("PINEPAY_API_KEY")}',
        'Content-Type': 'application/json'
    }
    data = {
        'amount': 10.50,
        'pixKey': 'user@example.com',
        'webhookUrl': 'https://your-domain.com/webhooks/pinepay',
        'externalId': 'order_123',
        'gateway': 'woovi',
        'description': 'Payment for order #123'
    }
    
    try:
        response = requests.post(url, json=data, headers=headers)
        response.raise_for_status()
        print('Payment created:', response.json())
        return response.json()
    except requests.exceptions.RequestException as e:
        print('Error creating payment:', e)
        return None

create_pix_payment()
```

```php PHP
<?php
$apiKey = $_ENV['PINEPAY_API_KEY'];
$url = 'https://api.pinepay.com/requestPayment';

$data = [
    'amount' => 10.50,
    'pixKey' => 'user@example.com',
    'webhookUrl' => 'https://your-domain.com/webhooks/pinepay',
    'externalId' => 'order_123',
    'gateway' => 'woovi',
    'description' => 'Payment for order #123'
];

$options = [
    'http' => [
        'header' => [
            'Authorization: Bearer ' . $apiKey,
            'Content-Type: application/json'
        ],
        'method' => 'POST',
        'content' => json_encode($data)
    ]
];

$context = stream_context_create($options);
$result = file_get_contents($url, false, $context);

if ($result === FALSE) {
    echo 'Error creating payment';
} else {
    echo 'Payment created: ' . $result;
}
?>
```
</CodeGroup>

## Step 4: Handle Webhooks

Set up a webhook endpoint to receive payment notifications:

<CodeGroup>
```javascript Node.js
const express = require('express');
const app = express();

app.use(express.json());

app.post('/webhooks/pinepay', (req, res) => {
  const { type, data } = req.body;
  
  if (type === 'PAYMENT_RECEIVED') {
    console.log('Payment received:', data);
    // Update your database, send confirmation email, etc.
  }
  
  res.status(200).json({ received: true });
});

app.listen(3000, () => {
  console.log('Webhook server running on port 3000');
});
```

```python Python
from flask import Flask, request, jsonify

app = Flask(__name__)

@app.route('/webhooks/pinepay', methods=['POST'])
def handle_webhook():
    data = request.get_json()
    webhook_type = data.get('type')
    
    if webhook_type == 'PAYMENT_RECEIVED':
        print('Payment received:', data.get('data'))
        # Update your database, send confirmation email, etc.
    
    return jsonify({'received': True})

if __name__ == '__main__':
    app.run(port=3000)
```

```php PHP
<?php
// webhook.php
$input = file_get_contents('php://input');
$data = json_decode($input, true);

$type = $data['type'] ?? null;

if ($type === 'PAYMENT_RECEIVED') {
    error_log('Payment received: ' . json_encode($data['data']));
    // Update your database, send confirmation email, etc.
}

http_response_code(200);
echo json_encode(['received' => true]);
?>
```
</CodeGroup>

## Step 5: Test Your Integration

<Steps>
<Step title="Create a Test Payment">
  Use the code examples above to create a test PIX payment with a small amount.
</Step>

<Step title="Check Payment Status">
  Use the transaction status endpoint to verify your payment was created successfully.
</Step>

<Step title="Simulate Webhook">
  Use tools like ngrok to expose your local webhook endpoint for testing.
</Step>

<Step title="Go Live">
  Once testing is complete, switch to production credentials and start processing real payments.
</Step>
</Steps>

## Next Steps

<CardGroup cols={2}>
<Card title="API Reference" icon="code" href="/api-reference">
Explore all available endpoints and their parameters.
</Card>

<Card title="Webhook Guide" icon="zap" href="/guides/webhook-handling">
Learn how to properly handle webhook notifications.
</Card>

<Card title="Error Handling" icon="alert-triangle" href="/guides/error-handling">
Understand common errors and how to handle them.
</Card>

<Card title="SDKs" icon="package" href="/sdks">
Use our official SDKs for faster integration.
</Card>
</CardGroup>

## Need Help?

<AccordionGroup>
<Accordion title="Common Issues">
  - **Authentication errors**: Verify your API key is correct and has proper permissions
  - **Webhook not received**: Ensure your endpoint is publicly accessible and returns 200 status
  - **Payment not processed**: Check if the gateway is active and properly configured
</Accordion>

<Accordion title="Testing Tips">
  - Use test mode for development and testing
  - Always validate webhook signatures in production
  - Implement proper error handling and retry logic
  - Monitor your webhook delivery success rates
</Accordion>
</AccordionGroup>
